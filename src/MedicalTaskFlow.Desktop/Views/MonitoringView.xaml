<UserControl x:Class="MedicalTaskFlow.Desktop.Views.MonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MedicalTaskFlow.Desktop.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MonitoringViewModel}">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Production Monitoring Dashboard" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>
        
        <!-- Metrics -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Border Background="#F44336" Padding="10" CornerRadius="3" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Errors" Foreground="White" FontWeight="Bold"/>
                    <TextBlock Text="{Binding ErrorCount}" Foreground="White" FontSize="24"/>
                </StackPanel>
            </Border>
            
            <Border Background="#FF9800" Padding="10" CornerRadius="3" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Warnings" Foreground="White" FontWeight="Bold"/>
                    <TextBlock Text="{Binding WarningCount}" Foreground="White" FontSize="24"/>
                </StackPanel>
            </Border>
            
            <Button Content="Refresh Logs" 
                    Command="{Binding LoadLogsCommand}" 
                    Padding="10,5"
                    Margin="10,0,0,0"/>
            
            <Button Content="Clear Logs" 
                    Command="{Binding ClearLogsCommand}" 
                    Padding="10,5"
                    Background="#F44336"
                    Foreground="White"
                    Margin="10,0,0,0"/>
        </StackPanel>
        
        <!-- Log List -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Logs}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" 
                                    Binding="{Binding Timestamp, StringFormat='{}{0:MM/dd HH:mm:ss}'}" 
                                    Width="140"/>
                
                <DataGridTextColumn Header="Level" 
                                    Binding="{Binding Level}" 
                                    Width="80"/>
                
                <DataGridTextColumn Header="Message" 
                                    Binding="{Binding Message}" 
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>